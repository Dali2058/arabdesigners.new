# Arab Designers v36

- Mobile responsive polish across messenger, publishing studio, crop modal, publish details modal, work tools and forms.
- Messenger conversation list is visible on mobile instead of being hidden, so incoming conversations can be identified from the unread notification.
- Messenger API keeps a conversation entry even if a historical sender profile has not synced yet, instead of dropping the conversation from the list.
- Messenger Profile button uses the current `?profile=username` share URL.
- YouTube social link now displays a YouTube SVG icon instead of `YT`; social pills are icon-only.
