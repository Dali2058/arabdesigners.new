const $=s=>document.querySelector(s);const app=$('#app');const toast=$('#toast');
const banner='https://i.postimg.cc/FFT9tnKW/banner.png';
let me=null;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function notify(m){toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2800)}
async function getMe(){try{const r=await fetch('/api/me');const d=await r.json();me=d.user}catch{} }
function nav(){return `<header class="nav glass"><a class="brand" href="/home"><img src="/assets/logo.png"><span>Arab Designers</span></a><nav class="navlinks"><a href="/home">Home</a><a href="/discover">Discover</a><a href="/designers">Designers</a><a href="/contact">Contact Us</a></nav><div class="spacer"></div><input class="search" id="globalSearch" placeholder="Search designers & designs"><button class="navbtn mobile" onclick="location.href='/discover'">Search</button><button class="navbtn" id="langBtn">EN / AR</button>${me?`<a class="btn" href="/settings">Settings</a><a class="btn" href="/admin">Admin</a><a href="/profile/${encodeURIComponent(me.username)}"><img class="avatar" src="${esc(me.avatar||'/assets/logo.png')}"></a>`:`<a class="btn primary" href="/login">Login</a>`}</header>`}
function shell(content){app.innerHTML=nav()+`<main class="container">${content}</main><footer class="footer">Arab Designers · A modern social platform for designers</footer>`;$('#langBtn')?.addEventListener('click',()=>notify('Arabic interface is ready to be enabled from the language system.'));$('#globalSearch')?.addEventListener('keydown',e=>{if(e.key==='Enter')location.href='/discover?q='+encodeURIComponent(e.target.value)})}
function home(){shell(`<section class="hero glass"><div><div class="eyebrow">The creative community</div><h1>Design.<br>Share.<br>Connect.</h1><p>Arab Designers is a modern social space built for designers, creators and the Arabic creative community.</p><div class="actions">${me?`<a class="btn primary" href="/profile/${encodeURIComponent(me.username)}">Open my profile</a>`:`<a class="btn primary" href="/login">Continue with Discord</a>`}<a class="btn" href="/discover">Explore designers</a></div></div><div class="hero-art"><div class="logo-card glass"><img src="/assets/logo.png"></div></div></section><section class="section"><div class="section-head"><div><div class="eyebrow">Built for creators</div><h2>A calmer way to showcase work</h2></div></div><div class="grid"><div class="card glass"><div class="stat">01</div><p class="muted">Discord-first authentication and profiles.</p></div><div class="card glass"><div class="stat">02</div><p class="muted">Visual portfolios designed around your work.</p></div><div class="card glass"><div class="stat">03</div><p class="muted">Discover designers and connect with the community.</p></div></div></section>`)}
function login(){app.innerHTML=`<main class="container login"><section class="login-card glass"><img src="/assets/logo.png"><div class="eyebrow">Arab Designers</div><h1>Welcome back.</h1><p>Sign in with Discord to create or access your designer profile. Your Discord username and display name stay connected to Discord.</p><div class="actions" style="justify-content:center"><a class="btn primary" href="/api/auth/discord">Continue with Discord</a><a class="btn" href="/home">Back</a></div></section></main>`}
async function profile(username){try{const r=await fetch('/api/profile/'+encodeURIComponent(username));if(!r.ok)throw 0;const d=(await r.json()).profile;const socials=(d.social_links||[]).map(x=>`<a class="pill" href="${esc(x.url)}" target="_blank" rel="noopener">${esc(x.platform)}</a>`).join('');const designs=(d.designs||[]).map(x=>`<article class="card design glass"><img src="${esc(x.image)}"><div class="meta"><strong>${esc(x.title)}</strong><div class="muted" style="margin-top:5px">Design</div></div></article>`).join('');shell(`<section class="profile glass"><div class="banner" style="background-image:url('${esc(d.banner||banner)}')"></div><div class="profile-body"><div class="profile-top"><img class="profile-avatar" src="${esc(d.avatar||'/assets/logo.png')}"><div><div class="profile-name">${esc(d.display_name)} ${d.verification_status==='verified'?'<span class="verified">✓</span>':''}</div><div class="muted">@${esc(d.username)}</div></div></div><p class="muted" style="max-width:760px;line-height:1.6">${esc(d.bio||'No bio yet.')}</p>${me&&String(me.id)===String(d.id)?`<div class="actions"><button class="btn" id="editProfile">Edit Profile</button><button class="btn primary" id="addDesign">Add Design</button></div>`:''}<div class="pills">${socials||'<span class="pill">No connected accounts</span>'}</div><div class="section"><div class="section-head"><h2>Designs</h2><span class="muted">${(d.designs||[]).length} works</span></div><div class="grid">${designs||'<div class="empty glass" style="grid-column:1/-1">No designs published yet.</div>'}</div></div></div></section>`); if(me&&String(me.id)===String(d.id)){ $('#editProfile').onclick=()=>editProfile(d); $('#addDesign').onclick=()=>addDesign(); }}catch{shell(`<div class="empty glass"><h2>Profile not found</h2><p class="muted">This designer does not exist.</p><a class="btn" href="/home">Go home</a></div>`)}}
function admin(){if(!me)return location.href='/login';shell(`<section class="form glass"><div class="eyebrow">Administration</div><h2>Admin Panel</h2><p class="muted">Protected server-side. Only configured admin emails can access this panel.</p><div id="adminData" class="section">Loading…</div></section>`);loadAdmin()}
async function loadAdmin(){const r=await fetch('/api/admin');const el=$('#adminData');if(!r.ok){el.textContent='Access denied.';return}const d=await r.json();el.innerHTML=`<div class="grid"><div class="card glass"><div class="stat">${d.users.length}</div><div class="muted">Users</div></div><div class="card glass"><div class="stat">${d.designCount}</div><div class="muted">Designs</div></div><div class="card glass"><div class="stat">${d.contacts.length}</div><div class="muted">Contact requests</div></div></div><div class="section">${d.users.map(u=>`<div class="card glass" style="margin-top:8px;display:flex;align-items:center;gap:12px"><img class="avatar" src="${esc(u.avatar||'/assets/logo.png')}"><div class="spacer"><strong>${esc(u.display_name)}</strong><div class="muted">@${esc(u.username)} · ${esc(u.verification_status)}</div></div><button class="btn" onclick="verifyUser(${u.id},'${u.verification_status==='verified'?'unverify':'verify'}')">${u.verification_status==='verified'?'Remove verification':'Verify'}</button></div>`).join('')}</div>`} window.verifyUser=async(id,action)=>{await fetch('/api/admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:id,action})});loadAdmin()};

function editProfile(d){shell(`<section class="form glass"><div class="eyebrow">Profile</div><h2>Edit Profile</h2><label class="label">Bio</label><textarea class="textarea" id="bio">${esc(d.bio||'')}</textarea><label class="label">Banner URL</label><input class="input" id="bannerUrl" value="${esc(d.banner||'')}" placeholder="https://..."><label class="label">Social links (one URL per line: Platform|URL)</label><textarea class="textarea" id="socials" placeholder="Behance|https://behance.net/...">${(d.social_links||[]).map(x=>x.platform+'|'+x.url).join('\n')}</textarea><div class="actions"><button class="btn primary" id="saveProfile">Save changes</button><a class="btn" href="/profile/${encodeURIComponent(d.username)}">Cancel</a></div></section>`);$('#saveProfile').onclick=async()=>{const lines=$('#socials').value.split('\n').map(x=>x.split('|')).filter(x=>x.length===2).map(x=>({platform:x[0].trim(),url:x[1].trim()}));const r=await fetch('/api/profile/update',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({bio:$('#bio').value,banner:$('#bannerUrl').value,socialLinks:lines})});if(r.ok){notify('Profile updated');location.href='/profile/'+encodeURIComponent(d.username)}else notify('Could not update profile')};}
function addDesign(){shell(`<section class="form glass"><div class="eyebrow">Portfolio</div><h2>Add a Design</h2><p class="muted">Choose an image and publish it to your portfolio.</p><label class="label">Title</label><input class="input" id="designTitle" placeholder="Project title" required><label class="label">Upload Image</label><input class="input" id="designFile" type="file" accept="image/png,image/jpeg,image/webp"><div id="preview" class="section"></div><div class="actions"><button class="btn primary" id="publish">Publish Design</button><a class="btn" href="/home">Cancel</a></div></section>`);let data='';$('#designFile').onchange=e=>{const f=e.target.files[0];if(!f)return;if(f.size>8*1024*1024)return notify('Please choose an image under 8MB.');const rd=new FileReader();rd.onload=()=>{data=rd.result;$('#preview').innerHTML=`<img class="glass" style="width:100%;max-height:420px;object-fit:contain;border-radius:20px" src="${data}">`};rd.readAsDataURL(f)};$('#publish').onclick=async()=>{if(!data)return notify('Select an image first.');const r=await fetch('/api/designs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:$('#designTitle').value,image:data})});if(r.ok){notify('Design published');location.href='/profile/'+encodeURIComponent(me.username)}else notify('Could not publish design')};}
function contact(){shell(`<section class="section"><div class="section-head"><div><div class="eyebrow">Get in touch</div><h2>What can we help you with?</h2><p class="muted">Choose a request type and our team will receive a structured Discord notification.</p></div></div><form id="contactForm" class="form glass"><label class="label">Request type</label><select class="select" name="type"><option>Apply Designer</option><option>Partnership</option><option>Contact Staff</option><option>Technical Support</option><option>Report</option><option>Other</option></select><label class="label">Email</label><input class="input" name="email" type="email" placeholder="you@example.com"><label class="label">Portfolio</label><input class="input" name="portfolio" placeholder="https://..."><label class="label">Message</label><textarea class="textarea" name="message" required placeholder="Tell us how we can help..."></textarea><div class="actions"><button class="btn primary" type="submit">Submit Request</button></div></form></section>`);$('#contactForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(r.ok){e.currentTarget.reset();notify('Request submitted successfully.')}else notify(d.error||'Something went wrong.')}}
async function discover(){let designs=[];try{const r=await fetch('/api/designs');designs=(await r.json()).designs||[]}catch{}shell(`<section class="section"><div class="section-head"><div><div class="eyebrow">Discover</div><h2>Featured work</h2><p class="muted">A visual feed for the Arab creative community.</p></div></div><div class="grid">${designs.map(x=>`<article class="card design glass"><img src="${esc(x.image)}"><div class="meta"><strong>${esc(x.title)}</strong><div class="muted" style="margin-top:5px">@${esc(x.username)}</div></div></article>`).join('')||'<div class="empty glass" style="grid-column:1/-1">No designs yet. Publish the first one from your profile.</div>'}</div></section>`)}
async function settings(){if(!me)return location.href='/login';shell(`<section class="form glass"><div class="eyebrow">Account</div><h2>Settings</h2><p class="muted">Manage your Arab Designers account.</p><div class="card glass" style="margin-top:18px"><strong>${esc(me.displayName)}</strong><div class="muted">@${esc(me.username)}</div></div><div class="actions"><button class="btn" id="logout">Logout</button></div></section>`);$('#logout').onclick=async()=>{await fetch('/api/auth/logout',{method:'POST'});location.href='/home'}}
async function route(){await getMe();const p=location.pathname;if(p==='/login')return login();if(p==='/contact')return contact();if(p==='/discover'||p==='/designers')return discover();if(p==='/settings')return settings();if(p==='/admin')return admin();if(p.startsWith('/profile/'))return profile(p.split('/').slice(2).join('/'));return home()}
window.addEventListener('popstate',route);document.addEventListener('click',e=>{const a=e.target.closest('a[href^="/"]');if(a&&!a.target&&a.getAttribute('href')!=='/api/auth/discord'){e.preventDefault();history.pushState({},'',a.href);route()}});route();
